# Copy to an ignored local file named authorized-target.env.
# The mentor must provide these values for the current assignment.

ROE_ACKNOWLEDGED=NO
AUTHORIZED_TARGET_HOST=assigned-pod.lab.example.invalid
AUTHORIZED_TARGET_SCHEME=https
AUTHORIZED_TARGET_PORT=443
MAX_REQUESTS_PER_SECOND=2
ASSIGNMENT_ID=replace-with-operator-issued-id

# Never add credentials, tokens, cookies or private keys to this file.
